
const CategoryDetail = async ({ param }) => {
    const { id } = await param;

    //fetch data from api
    // const category = await 
    return (
        <div></div>
    )
}

export default CategoryDetail